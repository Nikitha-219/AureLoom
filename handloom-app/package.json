{
  "name": "handloom-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend start",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "description": "Root folder for Handloom project containing frontend and backend",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
